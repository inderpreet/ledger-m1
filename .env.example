# Host port for the Caddy proxy (maps to container :80)
LEDGER_PORT=3000

# Set to 1 when HTTPS terminates in front of the app
LEDGER_SECURE_COOKIES=0

# Comma-separated browser origins if the UI is on another host
# LEDGER_CORS_ORIGINS=https://ledger.example.com
